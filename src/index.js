import { catsReducer } from './reducers'
import { createStore, applyMiddleware  } from 'redux'

window.catsReducer = catsReducer;
// window.counterReducer = counterReducer;
// window.getCats = getCats;
// window.store = store;
// window.actionCreators = actionCreators;
// window.myStore = myStore;
// window.generators = generators;